N, M = map(int, input().split())

for i in range(N // 2):
    pattern = '.|.' * (2 * i + 1)
    print(pattern.center(M, '-'))

print('WELCOME'.center(M, '-'))

for i in reversed(range(N // 2)):
    pattern = '.|.' * (2 * i + 1)
    print(pattern.center(M, '-'))
